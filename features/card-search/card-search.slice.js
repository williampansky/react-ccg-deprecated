import { createSlice } from '@reduxjs/toolkit';

let initialState = {};

const cardSearchSlice = createSlice({
  name: 'cardSearch',
  initialState,
  reducers: {}
});

// export const {} = cardSearchSlice.actions;
export default cardSearchSlice.reducer;
